# QA: Deterministic Systems

## Automated coverage

- [x] deterministic check context is embedded into AI requests
- [x] `GameEngine` keeps the client as the source of truth for resolved checks
- [x] resolved checks appear in the gameplay sidebar after a turn
- [x] detective campaigns keep irrelevant RPG chrome hidden in widget coverage
- [x] detective and narrative-only campaigns keep irrelevant RPG chrome hidden across repeated turns
- [x] full `flutter test` suite passes

## Manual checks still recommended

- [ ] long-play pass on fantasy campaigns with repeated checks and save/load cycles
- [ ] settings pass for runtime presets together with deterministic check flows

## Residual risks

- The current action-to-stat resolver is intentionally heuristic, not a full authored rules engine.
- Pure narrative campaigns without `Checks` stay safe, but deeper combat rules still belong to a future layer.
- `flutter analyze` still reports pre-existing warnings outside this slice, mainly in older prompt-builder files.
