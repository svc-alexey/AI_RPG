# QA: Campaign Modules

## Automated verification

- [x] `flutter pub run build_runner build --delete-conflicting-outputs`
- [x] `flutter test`
- [x] `flutter analyze` without new errors from Stage 6 changes
- [x] Widget and unit coverage for resolver, extraction, transient overlays, and module-aware sidebar rendering

## Covered scenarios

- [x] `detective` campaigns start without irrelevant `Inventory` or `Vitality` chrome
- [x] prompt-based setup can enable `Companions` and `Resources`
- [x] legacy saves infer active modules safely
- [x] structured roundtrip preserves activation reasons and module payloads
- [x] narration can unlock `Companions`
- [x] narration can unlock `Resources` and `Progression`
- [x] narration can unlock extracted recent `Checks`
- [x] chat sidebar renders only active modules
- [x] overlays surface state changes after a turn
- [x] sidebar briefly highlights modules changed by the latest turn
- [x] extracted check history appears in the sidebar after runtime activation

## Remaining manual checks

- [ ] manual pass: verify `fantasy` campaigns feel good with `Inventory` and `Vitality`
- [ ] manual pass: verify mobile drawer/sidebar remains comfortable with several active modules
- [ ] manual pass: verify check summaries stay readable in both `ru` and `en`

## Risks

- Story-driven auto-activation can still enable a module from a one-off narrative phrase if heuristics are too broad.
- Frequent state changes can turn lightweight overlays into noise if notification throttling is not refined further.
- The current `Checks` slice is extracted history, not deterministic resolution, so Stage 7 still carries the main gameplay-integrity risk.

## Verdict

- Acceptance status: Stage 6 implementation slices are in place and verified automatically.
- Remaining caveat: deterministic `Checks` resolution still belongs to Stage 7.
