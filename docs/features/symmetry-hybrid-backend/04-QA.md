# QA

## Automated checks

- [x] `flutter analyze`
- [x] `flutter test`
- [x] `python -m pytest tests` in `backend/symmetry`
- [x] `python -m compileall app alembic` in `backend/symmetry`

## Verified behaviors

- [x] register/login/refresh/logout works
- [x] campaign create/load/delete/process-turn works
- [x] important turn events are written into `world_chronicles`
- [x] story-template CRUD and interactions work
- [x] user-supplied provider credentials are not stored in DB or logs
- [x] Flutter runtime no longer depends on local campaign persistence
- [x] Alembic migration history is available and upgrade SQL can be generated

## Remaining manual follow-up

- [ ] live Yandex OAuth verification with real credentials
- [ ] deploy rehearsal against a remote server environment
