# Commands Protocol

Стартовый контекст для агентов и людей: [docs/AGENT_CONTEXT.md](../AGENT_CONTEXT.md) (продукт, карта кода, реестр фич, порядок чтения).

## Перезапуск приложения (остановить старые сессии и запустить заново)

Останавливает процессы на портах 8080/8081/8765 и запускает приложение:

```powershell
cd d:\AI_PRG; .\.cursor\skills\flutter-restart\scripts\restart.ps1
```

Для web-сервера (доступ по сети):

```powershell
cd d:\AI_PRG; .\.cursor\skills\flutter-restart\scripts\restart.ps1 -Web
```

---

## Запуск приложения для доступа по локальной сети

Чтобы открыть приложение с телефона или другого устройства в той же Wi‑Fi сети:

```bash
flutter run -d web-server --web-hostname 0.0.0.0 --web-port 8080
```

Затем откройте в браузере на любом устройстве: `http://<IP_вашего_ПК>:8080`

Узнать IP: `ipconfig` (Windows) или `ifconfig` (macOS/Linux).

---

## Общий принцип

Для каждой новой фичи работа идет не только по документам, но и по отдельным ролевым этапам.

Роли:

- архитектор
- аналитик
- разработчик
- тестировщик

## Автономный режим

Если пользователь формулирует запрос как выполнение фичи целиком, агент обязан:

1. сам добавить или обновить запись в [ImplementationPlan.md](D:/AI_PRG/ImplementationPlan.md)
2. сам зарегистрировать фичу в [docs/features/CATALOG.md](D:/AI_PRG/docs/features/CATALOG.md)
3. сам создать feature-папку
4. сам пройти роли:
   - архитектор
   - аналитик
   - разработчик
   - тестировщик
5. не ждать отдельного подтверждения между этапами, если нет скрытого риска или неоднозначного выбора

## Как запускать новую фичу

1. Создать запись в [ImplementationPlan.md](D:/AI_PRG/ImplementationPlan.md)
2. Создать запись в [docs/features/CATALOG.md](D:/AI_PRG/docs/features/CATALOG.md)
3. Создать git-ветку фичи в формате `codex/<feature-slug>`
4. Создать feature-папку по шаблону `docs/features/_template/`

## Ролевые этапы

### Архитектор

Артефакт:

- `01-Architecture.md`

### Аналитик

Артефакт:

- `02-PRD.md`

### Разработчик

Артефакты:

- `03-Implementation.md`
- кодовые изменения

### Тестировщик

Артефакт:

- `04-QA.md`

## Git Merge Policy

1. Каждая новая фича разрабатывается в отдельной git-ветке формата `codex/<feature-slug>`.
2. Реализация, проверки и feature-документация ведутся внутри этой feature-ветки.
3. Если фича завершена, прошла проверки и готова к приемке, агент обязан слить ее в `master`.
4. После merge в `master` агент обязан повторно прогнать проверки уже на объединенном результате.
5. `master` считается веткой только для стабильного, проверенного состояния проекта.
